package ERDiag::Utils::SchemaJSONParser;

use strict;


sub FromWeb($)
{
    my ($json) = @_;

    my $parsed_json = $json;
    
    #TODO Parse it

    return $parsed_json;
}

sub ToWeb($)
{
    my ($json) = @_;

    my $parsed_json = $json;
    
    #TODO Parse it

    return $parsed_json;
}
1;
